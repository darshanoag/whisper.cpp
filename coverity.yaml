analyze:
  location: local
capture:
  compiler-configuration:
    cov-configure:
      - ["--comptype=ti:cl2000", "--compiler=cl2000", "--template"]

  build:
    build-command: make
    clean-command: make clean
commit:
  connect:
    stream: urler
    url: http://192.168.0.24:8081
